# HOT — always-loaded memory

Re-injected every turn. Keep it small. For anything deeper, read the ONE named note.

## Protocol (short)
1. **Answer from here first** — it's already in context.
2. **One hop, not a scan** — need detail? Read the ONE named note. Never grep the vault mid-answer.
3. **Exact strings (URLs / IDs / fields) come from a READ of the note, never memory.**
4. **Verify** a named flag / field / endpoint against live state before acting.
5. **Flush** durable nuance into a note the moment you learn it (synthesis, not transcripts). Full rules → `brain/_PROTOCOL.md`.

## Standing rules (every turn)
- **External writes** (emails, notes, tickets, calendar): always confirm with [YOUR_NAME] first
- **Classify everything:** Questions, Decisions, Tasks, Commitments, Context, Projects
- **Update indexes immediately** when captures change

## Domains
_Add one-liners here as you build out brain notes — each line = a pointer to a deeper note._

[You'll build this out over time as your assistant learns your work.]
